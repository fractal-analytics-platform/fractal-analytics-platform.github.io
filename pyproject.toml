[project]
name = "fractal-analytics-platform-github-io"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Build
    "zensical==0.0.46",
    # Tasks-info retrieval
    "requests==2.34.2",
    "pydantic==2.13.4",
    "ruff==0.15.4",
    ]
